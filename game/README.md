# game/

Godot project will live here. Once `project.godot` exists in this folder, open Godot
and point "Import Project" at this directory.

Not started yet â€” current focus is the generation prototype in `../generation-service/`.

## Godot Sandbox Note

Godot only sees what is inside this folder when opened as a project. It will not crawl
up to `../generation-service/`. `res://` paths and export builds stay scoped to this
directory.
